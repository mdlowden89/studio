
'use client';

// This component is no longer in use as the onboarding flow has been removed.
// It is kept as an empty file to avoid breaking any potential (though unlikely) static imports.
export function OnboardingForm() {
  return null;
}
